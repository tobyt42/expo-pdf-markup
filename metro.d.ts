/**
 * Wraps a Metro config with the pdfjs-dist compatibility patches needed for
 * Expo web (Metro bundler). See metro.js for full documentation.
 */
export declare function withPdfMarkup(config: object): object;
